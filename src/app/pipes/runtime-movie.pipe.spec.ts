import { RuntimeMoviePipe } from './runtime-movie.pipe';

describe('RuntimeMoviePipe', () => {
  it('create an instance', () => {
    const pipe = new RuntimeMoviePipe();
    expect(pipe).toBeTruthy();
  });
});
